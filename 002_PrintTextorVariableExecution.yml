---
- name: Debug Module
  hosts: all
  vars:
    fruit: "elma"
  tasks:
    - name: debug message
      ansible.builtin.debug:
        msg: "Bu bir {{ fruit }}"
        verbosity: 2
    - name: debug message 02
      ansible.builtin.debug:
        msg: "Merhaba"
        verbosity: 2

